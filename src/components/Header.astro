---
interface Props {
  name: string;
  sections: { id: string }[];
}

const { name, sections } = Astro.props;
---

<header
  class="flex flex-col items-center gap-4 border-b border-[#363a4f] pb-4 lg:flex-row lg:justify-between lg:gap-6"
>
  <div
    class="flex min-w-0 w-full items-center justify-center gap-3 lg:w-auto lg:justify-start"
  >
    <a
      href="/"
      class="min-w-0 truncate text-lg font-bold tracking-[-0.02em] text-[#c6a0f6] no-underline sm:text-xl"
    >
      {name}
    </a>
  </div>

  <nav
    class="flex w-full flex-wrap items-center justify-center gap-x-5 gap-y-1 lg:w-auto lg:justify-end"
    aria-label="Main navigation"
  >
    {
      sections.map(({ id }) => (
        <a
          href={`/#${id}`}
          class="flex min-h-11 items-center justify-center border-b-2 border-transparent px-2 py-2 text-center text-sm font-semibold capitalize text-[#a5adcb] no-underline transition-colors hover:border-[#8aadf4] hover:text-[#cad3f5] active:border-[#8aadf4] active:text-[#cad3f5] sm:px-3"
        >
          {id.replaceAll("-", " ")}
        </a>
      ))
    }
    <button
      class="group grid h-11 w-11 shrink-0 place-items-center"
      id="cmd-button"
      type="button"
      aria-label="Open command palette"
      title="Open command palette (:)"
    >
      <img
        src="/terminal.svg"
        alt=""
        class="h-[1.125rem] w-[1.125rem] opacity-70 transition-opacity group-hover:opacity-100"
      />
    </button>
  </nav>
</header>
