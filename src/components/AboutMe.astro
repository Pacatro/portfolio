---
interface Props {
    title: string
    description: string;
}

const { title, description } = Astro.props;
---

<section class="about-me-section">
    <h2>{title}</h2>
    <p>{description}</p>
</section>